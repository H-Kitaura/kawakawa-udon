<div class="menu-title">
    <h3>メニュー一覧
    <p>
    サイズ 小=１玉 中=２玉、大=３玉
     </p>
     </h3>
</div>
  <div class="posting">
      <% if user_signed_in? && current_user.id %>
        <%= link_to "メニュー投稿",new_menu_path %>
      <% end %>
  </div>
  <div class="menus">
      <%= link_to "基本メニュー", menus_path,class:"menu" %>  
      <%= link_to "期間限定メニュー", index2_path,class:"menu" %>  
      <%= link_to "サイドメニュー", index3_path,class:"menu" %>  
  </div>

      <div class="menu-title">
        <h1>基本メニュー</h1>
      </div>
<div class="container">
  <div class="row">
  <% @food1.each do |food| %>
      <div class="col-md-4">
          <div class="card"%>
            <%= image_tag food.image %>
            <div class="card-body text-center">
            <h4 class="card-title"><%=food.title %></h4>
            <p class="card-text"><%=food.money %></p>
            <p class="card-text"><%=food.concept %></p>
                <% if user_signed_in? && current_user.id %>
                  <%= button_to "編集する", edit_menu_path(food.id),method: :get %>
                  <%= button_to "削除する", menu_path(food.id), method: :delete %>
                <% end %>
          </div>
      </div>
  </div>
  <%end%>
</div>
<%= paginate @food1 %>
