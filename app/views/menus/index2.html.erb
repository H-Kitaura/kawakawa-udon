<div class="menu-title">
    <h3>メニュー一覧
    <p>
    サイズ 小=１玉 中=２玉、大=３玉
     </p>
     </h3>
</div>
<div class="posting">
    <% if user_signed_in? && current_user.id %>
      <%= link_to "メニュー投稿",new_menu_path %>
    <% end %>
</div>
 
<div class="menus">
    <%= link_to "基本メニュー", menus_path,class:"menu" %>  
    <%= link_to "期間限定メニュー", index2_path,class:"menu" %>  
    <%= link_to "サイドメニュー", index3_path,class:"menu" %>  
</div>




    <div class="menu-title">
      <h1>期間限定メニュー</h1>
    </div>
     <div class="basic-card"> 
       <%  @food2.each do |food| %>
        <ul class = "card_lists">
          <li><%= image_tag food.image, class: :card__img  %></p>
              <div> 商品名</div>
              <%= food.title%></p>
              <div> 値段</div>
              <%= food.money%></p>
              <div> 商品紹介</div>
              <%= food.concept%></p>
            </li>
            <% if user_signed_in? && current_user.id %>
              <div class="food__manage">
              <%= link_to "編集する", edit_menu_path(food.id), class: :food__btn %>
              <%= link_to "削除する", menu_path(food.id), method: :delete, class: :food__btn %>
            <% end %>
        </ul>
       <% end %>
     </div>
    <%= paginate @food2 %>