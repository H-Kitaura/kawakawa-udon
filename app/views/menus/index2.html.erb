  <div class="row bg-light">
    <div class="col text-center">
        <h3>メニュー一覧 </h3>
        <p>
        サイズ 小=１玉 中=２玉、大=３玉
        </p>
      <div class="row card border-dark text-primary"">
        <%= link_to "基本メニュー", menus_path,class:"col" %>  
        <%= link_to "期間限定メニュー", index2_path,class:"col" %>  
        <%= link_to "サイドメニュー", index3_path,class:"col" %>  
      </div>
      <div class="row text-center bg-light m-4">
        <h1>期間限定メニュー</h1>
      </div>
  </div>
  </div>
<div class="container">
  <div class="row">
        <% if user_signed_in? && current_user.id %>
          <%= link_to "メニュー投稿",new_menu_path %>
        <% end %>
  </div>

  <div class="row">
  <% @food2.each do |food| %>
      <div class="col-md-4">
          <div class="card"%>
            <%= image_tag food.image %>
            <div class="card-body text-center">
            <h4 class="card-title"><%=food.title %></h4>
            <p class="card-text"><%=food.money %></p>
                <% if user_signed_in? && current_user.id %>
                  <%= button_to "編集する", edit_menu_path(food.id),method: :get %>
                  <%= button_to "削除する", menu_path(food.id), method: :delete %>
                <% end %>
          </div>
      </div>
  </div>
  <%end%>
</div>
<%= paginate @food2 %>
